var products = "1 for R3, 2 for R7, 3 for R10, 5 for R14.50.";

var number = [];

var number = products.split(",");
number.push();
console.log(number);

var newNumber = [];

number.forEach(function(product){
  var newNumber = number.split(" ");
if(newNumber.indexOf(number) === 0);
  newNumber.push(number);

});


for (var i = 0; i < number.length; i++){
 if(number.indexOf)
}
